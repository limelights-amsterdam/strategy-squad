# strategy-insights — the compounding store

The strategy-squad banks reusable insights here (one file per insight) and reads them before a new run,
so the squad gets sharper over time instead of starting cold. Format: see
`../../skills/strategy-compound/references/schema.md`. Categories live as subdirectories
(market-analysis/, positioning/, etc.). Keep entries client-anonymous if this repo is public.
